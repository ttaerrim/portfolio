type SectionProps = {
  title: string;
  description: string;
  githubUrl: string;
  url: string;
  stacks: string[];
  details: string[];
};

export default function ProjectCard({ title, description, githubUrl, url, stacks, details }: SectionProps) {
  return <section></section>;
}
